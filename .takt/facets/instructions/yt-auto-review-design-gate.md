Read and reconcile all three design-review reports:

{report:requirements-design-review.md}

{report:architecture-design-review.md}

{report:test-design-review.md}

Set structured verdict to `approve` only when requirements, architecture, and test design are all implementation-ready; set `reject` for repairable design findings; set `replan` when a premise or acceptance contract must change; otherwise set `abort`. Preserve every blocking finding.
