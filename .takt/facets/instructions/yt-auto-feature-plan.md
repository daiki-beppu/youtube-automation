Create the smallest implementation plan that satisfies the intake contract. Map each requirement to files, test-first evidence, failure handling, and completion checks; preserve repository boundaries and list explicit non-goals. Set structured verdict to `planned` when implementation can proceed, `complete` only for a non-implementation question, or `abort` when the accepted contract cannot be planned safely.
