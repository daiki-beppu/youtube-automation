Extend the existing ledger without deleting or summarizing prior entries. Audit definition/reference integrity, doctor/prompt composition, finite-stop contracts, lane boundaries, and repeated patterns in the bounded readable run set. Record unavailable run paths honestly.

Set structured verdict to `scope_complete` only when every inventoried target has evidence and a verdict, `progressed` when exact counts increased, or `abort` when no further evidence can be obtained.
