Plan a finite audit of the current `.takt/` definitions and readable run evidence. Inventory all public workflows, local step/facet references, main-checkout `.takt/runs`, and readable clone paths from `.takt/clone-meta/*.json`. Missing or swept run directories are recorded as unavailable, not invented.

Create the shared append-only audit plan and ledger formats. Set structured verdict to `planned`, or `abort` when neither definition evidence nor any bounded run evidence can be inspected.
