Define a finite audit scope and create the initial append-only ledger. Enumerate every target or a reproducible bounded discovery rule, the evidence required for each verdict, the order of inspection, and explicit completion counts. Do not edit product files.

Use the report formats exactly. Set structured verdict to `planned`, `complete` for a non-audit question, or `abort` when scope cannot be bounded.
