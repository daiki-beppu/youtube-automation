The accepted source ledger is `{report_dir}/audit-ledger.md`:

{report:audit-ledger.md}

Copy that ledger to the requested path under `docs/audits/` without changing its findings or evidence. Do not create GitHub issues, comments, commits, pushes, or pull requests.

Set structured verdict to `published` with the path and a byte/content consistency check, or `abort` when no accepted ledger exists or the destination is unsafe.
