Independently challenge the diagnosis. Check whether the reproduction proves the reported symptom, the causal chain predicts the evidence, competing hypotheses were genuinely tested, and the proposed red regression test would fail for the identified reason.

Return exactly one verdict: `APPROVE`, `REJECT` for repairable gaps, or `REDIAGNOSE` when the causal hypothesis must change. This is read-only and must fail closed on missing evidence.
