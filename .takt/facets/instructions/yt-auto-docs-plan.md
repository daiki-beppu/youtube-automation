Plan a documentation-only change. Enumerate the allowed files, the statements or user instructions that change, their authoritative sources, and the repository contract tests that verify consistency. Production code, runtime configuration, and generated artifacts are outside this lane. Set structured verdict to `planned` when ready, `complete` for a non-docs question, or `abort` when the result cannot stay within this lane.
