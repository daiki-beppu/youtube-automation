Read and reconcile all three diagnosis-review reports:

{report:causal-review.md}

{report:competing-hypothesis-review.md}

{report:regression-review.md}

Set structured verdict to `approve` only when the causal chain, competing-hypothesis evidence, and planned red regression test are coherent; set `reject` for repairable evidence gaps; set `rediagnose` when the causal hypothesis must change; otherwise set `abort`.
