Read and reconcile both documentation-review reports:

{report:docs-correctness-review.md}

{report:docs-consistency-review.md}

Set structured verdict to `approve` only when the diff is factual, consistent, and within the docs lane; set `reject` for repairable documentation findings; set `replan` when an authoritative premise differs; otherwise set `abort`.
