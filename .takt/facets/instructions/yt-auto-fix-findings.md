Read all current review and gate reports under `{report_dir}`, fix every blocking finding within the accepted scope, and rerun the narrow relevant checks. Set structured verdict to `fixed` when all blocking findings are resolved, `replan` when the implementation/maintenance plan must change, `rediagnose` when a bug's causal hypothesis must change, or `abort` when the findings cannot be resolved safely. Do not weaken or delete checks.
