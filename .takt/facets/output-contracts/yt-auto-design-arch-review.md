```markdown
# 設計レビュー

## 結果: APPROVE / REJECT / NEED_REPLAN

## サマリー

{1-2文で結果を要約}

## 要件ごとの設計判定

| 要件 ID   | 方針は実装が一意に決まる粒度か | 要求を満たすか | 判定 |
| --------- | ------------------------------ | -------------- | ---- |
| REQ-55-01 | はい                           | 満たす         | OK   |

## 確認した観点

- [x] 要求の充足
- [x] 責務の配置（domains / commands）
- [x] データの流れと SSOT
- [x] 失敗の設計
- [x] 変更の広がり
- [x] 未来への負債

## 今回の指摘（new）

| #   | finding_id        | 観点       | 対象             | 問題                                            | 修正案                      |
| --- | ----------------- | ---------- | ---------------- | ----------------------------------------------- | --------------------------- |
| 1   | DSN-NEW-REQ-55-02 | 責務の配置 | REQ-55-02 の方針 | 業務ロジックが CLI（commands）側に置かれている | domains のモジュール側へ移す |

## 継続指摘（persists）

| #   | finding_id | 観点 | 前回根拠 | 今回根拠 | 問題 | 修正案 |
| --- | ---------- | ---- | -------- | -------- | ---- | ------ |

## 解消済み（resolved）

| finding_id | 解消根拠 |
| ---------- | -------- |

## 非ブロッキング指摘（前進を止めない）

<!-- 収束ポリシー: 2 回目以降の新規指摘のうち無効化を示せないもの、形式・文書整備の指摘。spillover が起票の要否を判定する -->

| #   | finding_id | 対象 | 問題 | 修正案 | 無効化に当たらない理由 |
| --- | ---------- | ---- | ---- | ------ | ---------------------- |

## 再走査証跡（2回目以降のレビューで必須）

| 再照合した観点 | 根拠                             |
| -------------- | -------------------------------- |
| 要求の充足     | {全要件 ID を方針と突合した結果} |

## 判定条件

- 計画の前提そのものが成立しない（要件が矛盾する / ブリーフの決定では設計できない）→ NEED_REPLAN
- **ブロッキング**の `new` / `persists` が 1 件以上 → REJECT
- ブロッキングにできるのは、未解消の既出指摘（`persists`）と、設計の無効化を反例・観測で示せる新規指摘だけ（収束ポリシー）。それ以外の新規指摘は「非ブロッキング指摘」へ
- ブロッキング指摘なし → APPROVE（非ブロッキング指摘が残っていてもよい）
- `finding_id` なしの指摘は無効
```

**認知負荷軽減ルール:**

- APPROVE → サマリー + 要件ごとの設計判定 + 確認した観点 + 再走査証跡（2回目以降）+ 非ブロッキング指摘（あれば）
- REJECT / NEED_REPLAN → 該当指摘のみ表で記載（30行以内）
