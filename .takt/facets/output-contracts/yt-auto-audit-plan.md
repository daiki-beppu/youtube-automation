```markdown
# Audit Plan

## 監査観点と判定基準

<!-- order に判定基準があればそれを正とし、無ければ規約文書から導出して出典を書く -->

| #   | 観点   | OK の条件            | Finding の条件         | 出典                                        |
| --- | ------ | -------------------- | ---------------------- | ------------------------------------------- |
| 1   | {観点} | {何を満たせば OK か} | {何があれば Finding か} | {order.md の節 / 規約文書 `file` の該当決定} |

## 対象列挙方法

- Commands used:
  - `rg --files ...`
  - `...`
- Scope notes:
  - {order.md のスコープ指定と列挙結果の対応。スコープ外を加えない・減らさない}

## Audit Targets

| #   | Audit Target | 確認すること                             | 主要ファイル  | Priority            |
| --- | ------------ | ---------------------------------------- | ------------- | ------------------- |
| 1   | {監査対象}   | {判定基準のどの # をどう確認するか}      | `src/file.py` | High / Medium / Low |

## 監査順

- {監査順。High Priority から}

## スコープ外

- {対象} — {理由。order.md のどの記述に基づくか}

## Clarifications / Risks

- {確認事項や制約。なければ「なし」}
```

**Audit Targets の契約（後続の全工程がこの表を骨格として使う）:**

- 対象数は **28 以下**。超える場合は優先度の低い対象同士を統合する（max_steps 32、audit 1 パス最低 8 対象からの逆算値）
- 1 対象 = 監査者が 1 回のパスで関連ファイルを全文読み切れる粒度
- **# は以降の全レポートで不変**。監査台帳（02-audit-ledger.md）の監査台帳表はこの表と一対一（同じ # ・同じ行数・同じ対象名）で照合される
- 判定基準は判定者によって結論が割れない形で書く。「品質が十分」のような主観基準を残さない
