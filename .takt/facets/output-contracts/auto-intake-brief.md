```markdown
# 実装ブリーフ

## 判定: READY / BLOCKED

## 起点

| 項目  | 値                   |
| ----- | -------------------- |
| issue | `#<番号> <タイトル>` |

## 目的

{この変更で何が達成されるか。1-2 文}

## 確定している決定

<!-- issue 本文・コメントから -->

| 出典                              | 決定                                       |
| --------------------------------- | ------------------------------------------ |
| `#<番号> 本文` / `#<番号> コメント` | {決定の内容。実装が従うべき粒度まで具体に} |

## 要件候補

<!-- 受け入れ条件を 1 つ残らず。採番は計画 step が行うのでここでは番号を振らない -->

- [ ] {受け入れ条件そのまま}

## 対象外

<!-- issue が明示的に除外した範囲 -->

- {項目} — {除外理由}

## 制約

- {規約文書（CLAUDE.md / docs/development.md / docs/skill-design/skill-authoring-guidelines.md / 有効 ADR）や issue が課す制約}

## 着手を止める要因（BLOCKED のときのみ必須）

| #   | 種別                                                           | 引用              | 何が決まっていないか |
| --- | -------------------------------------------------------------- | ----------------- | -------------------- |
| 1   | 未決事項 / 曖昧な受け入れ条件 / 矛盾 / 未解消の依存 / 情報不足 | `#55 本文: "..."` | {不足の説明}         |

**次のアクション**: {人間が何をすれば着手可能になるか。「`gh issue comment <番号>` で未決事項の決定を追記する」等、具体のコマンドまで}
```

**認知負荷軽減ルール:**

- READY → 起点・目的・確定している決定・要件候補・対象外・制約。「着手を止める要因」は省略
- BLOCKED → 起点・着手を止める要因・次のアクション。要件候補は省略可
