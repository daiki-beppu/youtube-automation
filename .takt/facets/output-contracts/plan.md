```markdown
# タスク計画

## 元の要求
{ユーザーの要求をそのまま記載}

## 分析結果

### 目的
{達成すべきこと}

### 分解した要件
| # | 要件 | 種別 | 備考 |
|---|------|------|------|
| 1 | {要件1} | 明示 / 暗黙 | {複合要件を分解した場合の補足} |

- 1つの文に複数条件がある場合、検証可能な最小単位まで分解する
- `A/B`、`global/project`、`JSON/leaf`、`allow/deny`、`read/write` のような並列表現は別行にする

### 参照資料の調査結果（参照資料がある場合）
{参照資料の実装アプローチの概要と、現在の実装との主要な差異}

### スコープ
{影響範囲}

### 保持すべき既存動作
| 既存動作 | 根拠（ファイル:行） | 保持方法 |
|---------|-------------------|---------|
| {壊してはいけない動作} | {コード箇所} | {テストで保護 / 互換パス / 互換解決順序 / etc.} |

- plan step で実コードを確認して列挙する。推測で書かない
- 項目がない場合は「なし（新規機能のため既存動作への影響なし）」と明記する

### 依存・import 制約
| 制約 | 根拠（既存パターンのファイル:行） | 違反した場合の影響 |
|------|---------------------------|------------------|
| {守るべき制約} | {同種の既存実装} | {テスト失敗 / 起動時間劣化 / 型契約の破壊 / etc.} |

- 同種の既存実装の deps 宣言・import 方式を確認し、踏襲パターンを特定する
- 項目がない場合は「なし」と明記する

### 検討したアプローチ（設計判断がある場合）
| アプローチ | 採否 | 理由 |
|-----------|------|------|

### 実装アプローチ
{どう進めるか}

### 到達経路・起動条件（利用者向け機能の追加/変更がある場合）
| 項目 | 内容 |
|------|------|
| 利用者が到達する入口 | {メニュー/route/ボタン/リンク/外部呼び出し など。なければ「入口なし」と明記} |
| 更新が必要な呼び出し元・配線 | {更新対象のファイルや層} |
| 起動条件 | {認証、権限、URL条件、フラグなど} |
| 未対応項目 | {残っている配線があれば明記。なければ「なし」} |

## 実装ガイドライン（設計が必要な場合のみ）
- {Coderが実装時に従うべき指針}

## スコープ外（項目がある場合のみ）
| 項目 | 除外理由 |
|------|---------|

## 確認事項（あれば）
- {不明点や確認が必要な点}
```
