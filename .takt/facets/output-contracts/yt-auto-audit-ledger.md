```markdown
# Audit Ledger

監査済み: {n}/{N}

## Status: IN_PROGRESS / SCOPE_COMPLETE

## 監査台帳

| #   | Audit Target                                | 状態    | 判定              | 確認結果                                            | 根拠 (file:line)      | Finding    |
| --- | ------------------------------------------- | ------- | ----------------- | --------------------------------------------------- | --------------------- | ---------- |
| 1   | {計画レポート Audit Targets の #1 と同一名} | ✅ / ⏳ | OK / Finding / -- | {✅ の場合のみ、判定基準に対して何を確認したか。⏳ は --} | `src/file.py:42` {⏳ は --} | F{n} / -- |

## Findings（Issue 直貼り用）

### F{n}: {タイトル}

- 優先度: {高 / 中 / 低}
- 対象: #{Audit Targets の番号} {対象名}
- 場所: {`path/to/file.py:42`。複数可}
- 何が起きているか: {現状}
- なぜ問題か: {判定基準のどの # にどう反するか}
- 影響: {放置した場合に誰に何が起きるか}
- 推奨対応: {対応案}
- 完了条件: {条件}
- 確認方法: {方法}

## 集計

- 監査済み: {n}/{N}
- 判定別: OK {数} / Finding {数}
- 優先度別 Findings: 高 {数} / 中 {数} / 低 {数}

## 実行証跡

- {使用したコマンドと結果}
- {実行できなかった確認と理由（推測せず「未確認」と明記）}

## 残件

- 未監査の対象: {# の一覧 or なし}
```

**厳守ルール:**

- **この出力はファイルを完全に置き換える。** 応答に含めなかった行は物理的に消滅し、以後のステップから参照できなくなる。「既存の行を保持する」「他の行は変更しない」という**記述は保持にならない** — 保持したい行は 1 行残らずそのまま応答に再掲すること。差分・訂正のみの出力は台帳の破壊であり禁止。
- この台帳は追記専用。既存の ✅ 行・Findings・実行証跡は一字一句そのまま保持して全文を再掲する。要約・削除・言い換えによる縮小は禁止。
- 監査台帳表は計画レポート（01-audit-plan.md）の Audit Targets と**一対一**（同じ # ・同じ行数・同じ対象名）を常に保つ。行の削除・統合・番号の振り直しは禁止。未着手の対象も ⏳ で行を残す。
- ✅ へ変更してよいのは、このステップで対象の関連ファイルを実際に読んで監査した場合のみ。✅ を ⏳ に戻さない（単調増加）。
- 判定は OK / Finding の二値（⏳ 行は --）。OK 行にも根拠（`file:line`）を必ず書く。ファイルで示せない確認はコマンドを実行証跡に記録した上で、根拠欄にそのコマンドを書く。
- Finding 判定の行には F 番号を振り、Findings 節に 1 件ずつ詳細を書く。複数の問題を 1 つの F へ一括記載しない。Findings は台帳から Issue へそのまま貼り付けられる粒度を保つ。
- この台帳自身や Report Directory 内のファイル（`01-*.md` / `02-*.md` など）を場所とする Finding を書かない。Finding の対象はリポジトリの実ファイルだけ。
- 冒頭の進捗行「監査済み: {n}/{N}」は、毎回、表の実際の ✅ 行数と一致させる。**出力を確定する前に本文の行数を実際に数え**、進捗行・集計と一致することを自己検証する。
- Status を SCOPE_COMPLETE にできるのは、進捗行が N/N に達し ⏳ 行が残っていない場合のみ。それ以外は IN_PROGRESS のまま。
