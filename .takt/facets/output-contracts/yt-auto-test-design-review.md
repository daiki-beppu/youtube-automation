```markdown
# テスト設計レビュー

## 結果: APPROVE / REJECT

## サマリー

{1-2文で結果を要約}

## トレーサビリティ照合

| 要件 ID   | 対応ケース   | 判定                         |
| --------- | ------------ | ---------------------------- |
| REQ-55-01 | TC-01, TC-02 | OK                           |
| REQ-55-03 | なし         | NG（検証不能の理由も未記載） |

## 検出力の判定

<!-- 「このケースが通っても要件を満たさない実装が書けるか」を検証した結果 -->

| ケース ID | 落ちるべき実装の誤り | 実際に落ちるか | 判定 |
| --------- | -------------------- | -------------- | ---- |
| TC-01     | {誤りの例}           | 落ちる         | OK   |
| TC-04     | {誤りの例}           | 通ってしまう   | NG   |

## 確認した観点

- [x] 要件 ↔ ケースのトレーサビリティ
- [x] ケースの検出力
- [x] 境界・失敗経路
- [x] critical regression 回帰
- [x] テストファイル配置（`tests/test_*.py` と対象実装の対応）
- [x] 外部依存とモック方針
- [x] 再現テストと予測 ID の対応・修正前に red になる条件（fix のみ）
- [x] 診断が挙げた既存契約の回帰カバー（fix のみ）

## 今回の指摘（new）

| #   | finding_id       | 観点             | 対象      | 問題             | 修正案             |
| --- | ---------------- | ---------------- | --------- | ---------------- | ------------------ |
| 1   | TD-NEW-REQ-55-03 | トレーサビリティ | REQ-55-03 | 対応ケースがない | {設計すべきケース} |

## 継続指摘（persists）

| #   | finding_id | 観点 | 前回根拠 | 今回根拠 | 問題 | 修正案 |
| --- | ---------- | ---- | -------- | -------- | ---- | ------ |

## 解消済み（resolved）

| finding_id | 解消根拠 |
| ---------- | -------- |

## 非ブロッキング指摘（前進を止めない）

<!-- 収束ポリシー: 2 回目以降の新規指摘のうち無効化を示せないもの、形式・文書整備の指摘。spillover が起票の要否を判定する -->

| #   | finding_id | 対象 | 問題 | 修正案 | 無効化に当たらない理由 |
| --- | ---------- | ---- | ---- | ------ | ---------------------- |

## 再走査証跡（2回目以降のレビューで必須）

| 再照合した観点   | 根拠                               |
| ---------------- | ---------------------------------- |
| トレーサビリティ | {全要件 ID を対応表と突合した結果} |

## REJECT 判定条件

- **ブロッキング**の `new` / `persists` が 1 件以上ある場合のみ REJECT 可
- ブロッキングにできるのは、未解消の既出指摘（`persists`）と、設計の無効化（要件を満たさない実装が全ケースを通過する等）を反例で示せる新規指摘だけ（収束ポリシー）。それ以外の新規指摘は「非ブロッキング指摘」へ
- ブロッキング指摘なし → APPROVE（非ブロッキング指摘が残っていてもよい）
- `finding_id` なしの指摘は無効
```

**認知負荷軽減ルール:**

- APPROVE → サマリー + トレーサビリティ照合 + 確認した観点 + 再走査証跡（2回目以降）+ 非ブロッキング指摘（あれば）
- REJECT → 該当指摘のみ表で記載（30行以内）
