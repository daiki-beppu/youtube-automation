{
  "name": "tayk",
  "version": "0.1.0-alpha.0",
  "description": "tayk command line entry point for the youtube-channels-automation TS rewrite (ADR-0004 citty dispatcher).",
  "bin": {
    "tayk": "./dist/cli.js"
  },
  "files": [
    "dist",
    "_skills",
    "_claude_md"
  ],
  "type": "module",
  "scripts": {
    "build": "bun build ./bin/tayk.ts --target=bun --outfile=dist/cli.js --external sharp --banner '#!/usr/bin/env bun'",
    "prepack": "bun run build && bun run scripts/bundle-symlinks.ts materialize",
    "postpack": "bun run scripts/bundle-symlinks.ts restore"
  },
  "devDependencies": {
    "citty": "^0.2.2"
  },
  "optionalDependencies": {
    "sharp": "^0.34.0"
  }
}
